title = Forcepoint - DNS over HTTPS (DoH) Domain Access
description = Detects access to known DNS over HTTPS (DoH) resolver domains (e.g. Cloudflare, Google, Quad9) which are frequently used to bypass local DNS filters.
